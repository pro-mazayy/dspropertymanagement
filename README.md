# Dreamstate Property Management | ROI Domination

Premium property management platform built for maximum yield and market domination.

## Strategic Vision
We don't just "manage" properties. We weaponize them using data-driven AI pricing, psychological listing optimization, and elite operational infrastructure to crush the competition.

## Technologies
- **Vite** (Next-gen Frontend Tooling)
- **TypeScript** (Static Typing)
- **React** (Component Architecture)
- **Tailwind CSS** (Utility-First Styling)
- **Framer Motion** (High-Impact Animations)
- **Supabase** (Lead Capture and Backend)
- **Shadcn UI** (Premium Component Library)

## Local Deployment

1. **Clone the repository**
2. **Install dependencies**: `npm i`
3. **Environment setup**: Create a `.env` file with your Supabase credentials.
   ```env
   VITE_SUPABASE_URL=your_url
   VITE_SUPABASE_PUBLISHABLE_KEY=your_key
   ```
4. **Start the engine**: `npm run dev`

## Deployment
This project is configured for Vercel. Ensure you set the `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` environment variables in your Vercel dashboard to prevent initialization errors.
